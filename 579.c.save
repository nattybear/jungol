#include <stdio.h>

int des(int a)
{
  int pr;
  int arr[a];
  for (int i = 0; i < a - 1; i++)
    scanf("%d ", &arr[i]);
  pr = arr[0];
  for (int i = 0; i < a - 1; i++) {
      pr = arr[i];
     if (arr[i] < arr[i+1]) {
      arr[i] = arr[i+1]; arr[i+1] = pr;
    }
  }
  for (int i = 0; i < a - 1; i++)
    
printf("%d ", arr[i]);
  return 0;
}

int main()
{
  int e=0;
  scanf("%d\n", &e);
  des(e);
  return 0;
}
